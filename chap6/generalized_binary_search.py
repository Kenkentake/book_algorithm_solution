def P(x: int) -> bool:
    pass

def binary_search():
    while ridge - left > 1:
        mid = left + (right - left) // 2
        if P(mid):
            right = mid
        else:
            left = mid
    return right
